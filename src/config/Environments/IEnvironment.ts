export interface IEnvironment {
  PORT : number;
  JWT_KEYWORD : string;
  NODE_ENV: string;
  DB_TYPE: any;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_NAME: string;
  DB_PORT: number;
  DB_HOST: string;
  DB_STRING_CNN: string;
  CORS_ORIGIN  : string;
}

